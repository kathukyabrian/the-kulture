import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'tech.kitucode.kulture',
  appName: 'The Kulture',
  webDir: 'dist/app/browser'
};

export default config;
